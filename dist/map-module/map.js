/* @skipi/map dist — generated by build.sh, do not edit */
/*
 * @skipi/map — gazetteer (text → coordinates)
 *
 * The whole Skipi ecosystem stores geography as TEXT (ports, country codes,
 * trading areas), never lat/lon. This gazetteer turns that text into map
 * points so every home's data becomes mappable. Seeded from the broker map's
 * PORT_COORDS; homes may merge extra coords via host.gazetteer.
 *
 * Classic browser script — attaches to window.SkipiMap.gazetteer. Also safe
 * to load under Node (attaches to globalThis) for contract tests.
 */
(function (global) {
  'use strict';

var PORT_COORDS = {
    "constanta":[44.17,28.65], "constantza":[44.17,28.65], "burgas":[42.50,27.47],
    "varna":[43.21,27.92], "odessa":[46.49,30.74], "odesa":[46.49,30.74], "yuzhny":[46.62,31.10],
    "chornomorsk":[46.27,30.65], "illichevsk":[46.31,30.66], "pivdennyi":[46.62,31.10],
    "kandla":[22.97,70.22], "mundra":[22.83,69.72], "paradip":[20.27,86.61], "visakhapatnam":[17.69,83.30],
    "chennai":[13.08,80.27], "tuticorin":[8.78,78.13], "krishnapatnam":[14.27,80.13], "mumbai":[18.94,72.84],
    "dar es salaam":[-6.80,39.30], "tanga":[-5.07,39.10], "mtwara":[-10.27,40.18],
    "fos":[43.43,4.95], "marseille":[43.30,5.36],
    "novorossiysk":[44.72,37.78], "tuapse":[44.10,39.07], "kavkaz":[45.34,36.69],
    "yeisk":[46.71,38.27], "rostov":[47.23,39.72], "taganrog":[47.22,38.91],
    "mariupol":[47.10,37.55], "kerch":[45.36,36.47], "sevastopol":[44.62,33.53],
    "sulina":[45.16,29.65], "braila":[45.27,27.96], "galati":[45.43,28.04],
    "izmail":[45.35,28.84], "reni":[45.45,28.28], "samsun":[41.29,36.33],
    "trabzon":[41.00,39.72], "zonguldak":[41.45,31.80],
    "marmara":[40.97,28.95], "istanbul":[41.01,28.97], "izmit":[40.77,29.94],
    "tekirdag":[40.98,27.51], "bandirma":[40.35,27.97], "gemlik":[40.43,29.16],
    "aliaga":[38.80,26.95], "izmir":[38.42,27.14], "piraeus":[37.94,23.65],
    "thessaloniki":[40.63,22.95], "limassol":[34.67,33.04], "larnaca":[34.92,33.63],
    "mersin":[36.79,34.62], "iskenderun":[36.59,36.18], "aegean":[38.50,25.50],
    "alexandria":[31.21,29.92], "damietta":[31.42,31.81], "portsaid":[31.26,32.30],
    "port said":[31.26,32.30], "suez":[29.97,32.55], "haifa":[32.82,35.00],
    "ashdod":[31.81,34.65],
    "trieste":[45.65,13.78], "venice":[45.43,12.34], "ravenna":[44.42,12.20],
    "ancona":[43.62,13.51], "civitavecchia":[42.10,11.79], "augusta":[37.21,15.22],
    "cagliari":[39.21,9.11], "naples":[40.83,14.25], "valletta":[35.90,14.51],
    "tarragona":[41.11,1.25], "barcelona":[41.34,2.17], "valencia":[39.45,-0.32],
    "cartagena":[37.60,-0.98], "algeciras":[36.13,-5.45], "sagunto":[39.65,-0.21],
    "sibenik":[43.74,15.89], "split":[43.51,16.43], "koper":[45.55,13.73],
    "rijeka":[45.32,14.45], "ploce":[43.06,17.43], "bar":[42.09,19.10],
    "tunis":[36.79,10.30], "sfax":[34.73,10.76], "bejaia":[36.75,5.07],
    "algiers":[36.78,3.06], "oran":[35.71,-0.65], "annaba":[36.90,7.77],
    "casablanca":[33.61,-7.62], "agadir":[30.42,-9.59], "med":[37.00,18.00],
    "lisbon":[38.71,-9.14], "sines":[37.95,-8.87], "setubal":[38.52,-8.89],
    "la coruna":[43.37,-8.40], "bilbao":[43.34,-3.04], "rotterdam":[51.92,4.48],
    "amsterdam":[52.40,4.85], "antwerp":[51.22,4.42], "ghent":[51.10,3.74],
    "hamburg":[53.55,9.99], "bremen":[53.10,8.74], "gdansk":[54.36,18.65],
    "klaipeda":[55.71,21.13], "riga":[57.00,24.05], "tallinn":[59.44,24.75],
    "ust luga":[59.66,28.27], "primorsk":[60.36,28.61],
    "spb":[59.94,30.31], "st petersburg":[59.94,30.31], "saint petersburg":[59.94,30.31],
    "petersburg":[59.94,30.31], "leningrad":[59.94,30.31],
    "azov":[47.11,39.42], "vyborg":[60.71,28.75],
    "kaliningrad":[54.71,20.51], "baltiysk":[54.65,19.91],
    "vladivostok":[43.12,131.89], "nakhodka":[42.82,132.88],
    "vostochny":[42.74,133.07], "korsakov":[46.63,142.78],
    "murmansk":[68.97,33.08], "arkhangelsk":[64.55,40.54],
    "vanino":[49.10,140.27], "magadan":[59.57,150.80], "sakhalin":[47.00,142.50],
    "taman":[45.21,36.71], "temryuk":[45.27,37.39],
    "dakar":[14.69,-17.45], "abidjan":[5.33,-4.03], "tema":[5.62,0.00],
    "lome":[6.13,1.22], "cotonou":[6.36,2.43], "lagos":[6.45,3.40],
    "onne":[4.74,7.16], "douala":[4.05,9.69], "pointe noire":[-4.78,11.86],
    "pointenoire":[-4.78,11.86], "luanda":[-8.79,13.24], "walvis bay":[-22.95,14.50],
    "waf":[5.00,3.00],
    "kandla":[22.99,70.22], "mundra":[22.84,69.71], "pipavav":[20.93,71.50],
    "mumbai":[18.95,72.83], "jnpt":[18.95,72.95], "navasheva":[18.95,72.95],
    "mormugao":[15.41,73.81], "mangalore":[12.89,74.84], "cochin":[9.97,76.27],
    "tuticorin":[8.78,78.20], "chennai":[13.10,80.30], "kakinada":[16.97,82.25],
    "kakinada anchor":[16.97,82.25], "visakhapatnam":[17.69,83.22],
    "visakhapatnam outer":[17.68,83.30], "paradip":[20.27,86.69],
    "haldia":[22.04,88.07], "kolkata":[22.55,88.34], "krishnapatnam":[14.25,80.10],
    "wci":[20.00,72.00], "eci":[15.00,82.00],
    "fujairah":[25.13,56.34], "khor fakkan":[25.34,56.36], "jebel ali":[25.02,55.06],
    "dubai":[25.27,55.30], "bahrain":[26.20,50.59], "dammam":[26.45,50.10],
    "jubail":[27.00,49.66], "jeddah":[21.49,39.16], "yanbu":[24.09,38.06],
    "aqaba":[29.53,35.00], "muscat":[23.61,58.59], "sohar":[24.36,56.71],
    "salalah":[16.94,54.01],
    "djibouti":[11.59,43.15], "berbera":[10.43,45.02], "mombasa":[-4.04,39.66],
    "dar es salaam":[-6.80,39.30], "maputo":[-25.96,32.59], "durban":[-29.87,31.04],
    "richards bay":[-28.79,32.07], "cape town":[-33.92,18.42],
    "singapore":[1.29,103.85], "port kelang":[2.95,101.40], "port klang":[2.95,101.40],
    "tanjung pelepas":[1.36,103.55], "manila":[14.60,120.97], "subic bay":[14.79,120.27],
    "ho chi minh":[10.78,106.70], "haiphong":[20.86,106.69], "bangkok":[13.72,100.49],
    "laem chabang":[13.07,100.88],
    "shanghai":[31.23,121.47], "ningbo":[29.87,121.55], "zhoushan":[30.00,122.10],
    "zhousan":[30.00,122.10], "qingdao":[36.07,120.38], "tianjin":[39.13,117.20],
    "dalian":[38.92,121.61], "yantai":[37.46,121.45], "rizhao":[35.42,119.46],
    "qinhuangdao":[39.93,119.62], "lianyungang":[34.60,119.16],
    "guangzhou":[23.13,113.27], "shenzhen":[22.55,114.06], "xiamen":[24.48,118.08],
    "fuzhou":[26.07,119.32], "hong kong":[22.32,114.17], "hongkong":[22.32,114.17],
    "dung quat":[15.38,108.79], "north china":[38.00,120.00], "ne china":[39.00,121.00],
    "south china":[23.00,113.00],
    "yokohama":[35.45,139.65], "tokyo":[35.66,139.79], "nagoya":[35.10,136.88],
    "osaka":[34.65,135.43], "kobe":[34.69,135.20], "chiba":[35.60,140.10],
    "kashima":[35.93,140.69], "busan":[35.10,129.04], "incheon":[37.46,126.62],
    "ulsan":[35.50,129.39], "pohang":[36.04,129.38], "gwangyang":[34.90,127.74],
    "japan":[35.50,139.00],
    "newcastle":[-32.92,151.78], "port hedland":[-20.31,118.58],
    "dampier":[-20.66,116.71], "fremantle":[-32.05,115.74],
    "gladstone":[-23.84,151.25], "townsville":[-19.27,146.83],
    "abbot point":[-19.87,148.07], "kembla":[-34.48,150.92],
    "west black sea":[42.50,28.00], "east black sea":[43.50,38.00],
    "north black sea":[46.00,32.00], "south black sea":[42.00,33.00],
    "houston":[29.74,-95.30], "corpus christi":[27.81,-97.40], "new orleans":[29.95,-90.07],
    "nola":[29.95,-90.07], "tampa":[27.95,-82.46], "savannah":[32.08,-81.10],
    "charleston":[32.78,-79.93], "norfolk":[36.85,-76.29], "baltimore":[39.29,-76.61],
    "new york":[40.71,-74.01], "santos":[-23.97,-46.33], "itaqui":[-2.57,-44.37],
    "vitoria":[-20.32,-40.31], "praia mole":[-20.28,-40.23], "tubarao":[-20.27,-40.24],
    "ponta da madeira":[-2.57,-44.36], "buenos aires":[-34.61,-58.38],
    "rosario":[-32.95,-60.66], "san lorenzo":[-32.74,-60.74], "necochea":[-38.55,-58.74],
    "bahia blanca":[-38.72,-62.27], "paranagua":[-25.51,-48.51],
    "gibraltar":[36.14,-5.35], "canaries":[28.10,-15.50], "azores":[37.74,-25.66],
};

  // Coarse country centroids for the region layer fallback + hub labels.
  // Not exhaustive — extend via host.gazetteer.countries.
  var COUNTRY_CENTROIDS = {
    "ukraine":[48.38,31.17],"russia":[61.52,105.32],"turkey":[38.96,35.24],
    "greece":[39.07,21.82],"italy":[41.87,12.57],"spain":[40.46,-3.75],
    "netherlands":[52.13,5.29],"germany":[51.17,10.45],"poland":[51.92,19.15],
    "romania":[45.94,24.97],"bulgaria":[42.73,25.49],"georgia":[42.32,43.36],
    "cyprus":[35.13,33.43],"malta":[35.94,14.38],"egypt":[26.82,30.80],
    "india":[20.59,78.96],"china":[35.86,104.20],"singapore":[1.35,103.82],
    "philippines":[12.88,121.77],"indonesia":[-0.79,113.92],"japan":[36.20,138.25],
    "south korea":[35.91,127.77],"vietnam":[14.06,108.28],"usa":[37.09,-95.71],
    "united states":[37.09,-95.71],"brazil":[-14.24,-51.93],"argentina":[-38.42,-63.62],
    "south africa":[-30.56,22.94],"nigeria":[9.08,8.68],"morocco":[31.79,-7.09],
    "panama":[8.54,-80.78],"liberia":[6.43,-9.43],"marshall islands":[7.13,171.18],
    "croatia":[45.10,15.20],"united kingdom":[55.38,-3.44],"france":[46.23,2.21],
    "norway":[60.47,8.47],"estonia":[58.60,25.01],"latvia":[56.88,24.60],
    "lithuania":[55.17,23.88]
  };

  function _norm(raw) {
    if (!raw) return '';
    return String(raw).toLowerCase().replace(/[^a-z0-9 ]/g, '').replace(/\s+/g, ' ').trim();
  }

  // Fuzzy port resolver (from broker _portCoords): exact, then longest
  // leading-token prefix, then first token. Returns [lat,lon] or null.
  function resolvePort(raw) {
    var key = _norm(raw);
    if (!key) return null;
    if (PORT_COORDS[key]) return PORT_COORDS[key];
    var tokens = key.split(' ');
    for (var n = tokens.length; n > 0; n--) {
      var head = tokens.slice(0, n).join(' ');
      if (PORT_COORDS[head]) return PORT_COORDS[head];
      var compact = head.replace(/\s+/g, '');
      if (PORT_COORDS[compact]) return PORT_COORDS[compact];
    }
    if (PORT_COORDS[tokens[0]]) return PORT_COORDS[tokens[0]];
    return null;
  }

  function resolveCountry(raw) {
    var key = _norm(raw);
    if (!key) return null;
    return COUNTRY_CENTROIDS[key] || null;
  }

  // Resolve any place text: try port first, then country centroid.
  function resolve(raw) {
    return resolvePort(raw) || resolveCountry(raw);
  }

  // Homes extend coverage without forking: merge extra {port:[lat,lon]} maps.
  function merge(extraPorts, extraCountries) {
    if (extraPorts) { for (var p in extraPorts) if (extraPorts.hasOwnProperty(p)) PORT_COORDS[_norm(p)] = extraPorts[p]; }
    if (extraCountries) { for (var c in extraCountries) if (extraCountries.hasOwnProperty(c)) COUNTRY_CENTROIDS[_norm(c)] = extraCountries[c]; }
  }

  global.SkipiMap = global.SkipiMap || {};
  global.SkipiMap.gazetteer = {
    ports: PORT_COORDS,
    countries: COUNTRY_CENTROIDS,
    norm: _norm,
    resolvePort: resolvePort,
    resolveCountry: resolveCountry,
    resolve: resolve,
    merge: merge
  };
})(typeof window !== 'undefined' ? window : globalThis);

/*
 * @skipi/map — map-core (generic layered map engine)
 *
 * A framework-free Leaflet engine shared by every Skipi home. It knows NOTHING
 * about any home's globals (no `state`, `invoke`, `apiFetch`). A home talks to
 * it ONLY through the injected `host` contract (see CONTRACT.md):
 *
 *   SkipiMap.mount(container, {
 *     layers: [ { id, label, kind, provider, visible?, style?, onSelect? } ],
 *     worldGeoJsonUrl?, gazetteer?, t?, toast?, onSelect?
 *   }) -> instance
 *
 *   provider() => Promise<Feature[]>   // home supplies its own data here
 *
 * Feature shapes by kind:
 *   ports:   { id, port, label?, meta? }
 *   vessels: { id, imo?, name, port?|latlng?, status?, meta? }
 *   routes:  { id, from, to, label?, meta? }
 *   regions: { id, country, value?, label?, meta? }
 *   geo:     { id, label?, title?, text?, latlng?|[lat,lon], geometry? (GeoJSON), meta? }
 *            — free-form hazards / MSI / any pre-geocoded overlay
 *
 * Base map is OFFLINE by default (bundled Natural Earth ~50m land polygons).
 * Optional host.tileUrl enables raster tiles under the land layer when the
 * home wants more detail at high zoom (network). Local-first remains default.
 *
 * Classic browser script — needs Leaflet (`L`) loaded first. Also loadable in
 * Node with an `L` stub for contract tests.
 */
(function (global) {
  'use strict';

  var VERSION = '0.2.3';
  var KINDS = {};

  function registerKind(name, fn) { KINDS[name] = fn; }

  function esc(s) {
    return String(s == null ? '' : s).replace(/[&<>"]/g, function (c) {
      return { '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;' }[c];
    });
  }

  // Sequential colour ramp for choropleth values (0..1) — colour-blind safe-ish.
  function ramp(t) {
    t = Math.max(0, Math.min(1, t));
    var stops = ['#eef6ff', '#c6dbef', '#88b7e0', '#4a90d9', '#1f5fa8'];
    var i = Math.min(stops.length - 2, Math.floor(t * (stops.length - 1)));
    return stops[i + (t >= (i + 1) / (stops.length - 1) ? 1 : 0)] || stops[i];
  }

  // ---------------------------------------------------------------- kinds ----

  registerKind('ports', function (group, feats, cfg, ctx) {
    var rendered = 0, skipped = 0, style = cfg.style || {};
    feats.forEach(function (f) {
      var ll = f.latlng || ctx.gazetteer.resolvePort(f.port);
      if (!ll) { skipped++; return; }
      var m = ctx.L.circleMarker(ll, Object.assign({
        radius: 6, color: '#1f5fa8', weight: 2, fillColor: '#4a90d9', fillOpacity: 0.85
      }, style));
      m.bindPopup('<b>' + esc(f.label || f.port) + '</b>');
      m.on('click', function () { ctx.emitSelect(cfg.id, f); });
      group.addLayer(m);
      rendered++;
    });
    return { rendered: rendered, skipped: skipped };
  });

  registerKind('vessels', function (group, feats, cfg, ctx) {
    var rendered = 0, skipped = 0, style = cfg.style || {};
    feats.forEach(function (f) {
      var ll = f.latlng || ctx.gazetteer.resolvePort(f.port);
      if (!ll) { skipped++; return; }
      var m = ctx.L.circleMarker(ll, Object.assign({
        radius: 5, color: '#8a5a00', weight: 2, fillColor: '#f0a92b', fillOpacity: 0.9
      }, style));
      var html = '<b>' + esc(f.name || ('IMO ' + (f.imo || '?'))) + '</b>';
      if (f.imo) html += '<br>IMO ' + esc(f.imo);
      if (f.status) html += '<br>' + esc(f.status);
      m.bindPopup(html);
      m.on('click', function () { ctx.emitSelect(cfg.id, f); });
      group.addLayer(m);
      rendered++;
    });
    return { rendered: rendered, skipped: skipped };
  });

  registerKind('routes', function (group, feats, cfg, ctx) {
    var rendered = 0, skipped = 0, style = cfg.style || {};
    feats.forEach(function (f) {
      var a = f.fromLatlng || ctx.gazetteer.resolvePort(f.from);
      var b = f.toLatlng || ctx.gazetteer.resolvePort(f.to);
      if (!a || !b) { skipped++; return; }
      var line = ctx.L.polyline([a, b], Object.assign({
        color: '#1f5fa8', weight: 2, opacity: 0.7, dashArray: '6 5'
      }, style));
      line.bindPopup('<b>' + esc(f.label || (f.from + ' → ' + f.to)) + '</b>');
      line.on('click', function () { ctx.emitSelect(cfg.id, f); });
      group.addLayer(line);
      // endpoint dots to anchor the eye
      group.addLayer(ctx.L.circleMarker(a, { radius: 3, color: '#1f5fa8', fillColor: '#1f5fa8', fillOpacity: 1, weight: 1 }));
      group.addLayer(ctx.L.circleMarker(b, { radius: 3, color: '#b23', fillColor: '#b23', fillOpacity: 1, weight: 1 }));
      rendered++;
    });
    return { rendered: rendered, skipped: skipped };
  });

  registerKind('regions', function (group, feats, cfg, ctx) {
    var world = ctx.worldGeo;
    if (!world) return { rendered: 0, skipped: feats.length };
    var norm = ctx.gazetteer.norm;
    var wanted = {}, vals = [];
    feats.forEach(function (f) {
      wanted[norm(f.country)] = f;
      if (typeof f.value === 'number') vals.push(f.value);
    });
    var max = vals.length ? Math.max.apply(null, vals) : 0;
    var rendered = 0;
    var gj = ctx.L.geoJSON(world, {
      filter: function (feat) { return Object.prototype.hasOwnProperty.call(wanted, norm(feat.properties && feat.properties.n)); },
      style: function (feat) {
        var f = wanted[norm(feat.properties.n)];
        var t = (max > 0 && typeof f.value === 'number') ? f.value / max : 0.5;
        return { color: '#1f5fa8', weight: 1, fillColor: ramp(t), fillOpacity: 0.55 };
      },
      onEachFeature: function (feat, lyr) {
        var f = wanted[norm(feat.properties.n)];
        rendered++;
        lyr.bindPopup('<b>' + esc(f.label || feat.properties.n) + '</b>' + (typeof f.value === 'number' ? '<br>' + esc(f.value) : ''));
        lyr.on('click', function () { ctx.emitSelect(cfg.id, f); });
      }
    });
    group.addLayer(gj);
    return { rendered: rendered, skipped: feats.length - Object.keys(wanted).length };
  });

  /**
   * S-52-inspired MSI palette (NOT certified ECDIS Presentation Library).
   * Magenta ≈ ECDIS caution; orange ≈ mariner NINFO; red ≈ SAR urgency.
   */
  var MSI_S52 = {
    NAV:   { color: '#c000c0', fill: '#c000c0', fillOpacity: 0.16, dash: '8 5',  mark: '!',  name: 'Navigational' },
    MET:   { color: '#2070b8', fill: '#2070b8', fillOpacity: 0.14, dash: '6 4',  mark: 'W',  name: 'Meteorological' },
    SAR:   { color: '#c02828', fill: '#c02828', fillOpacity: 0.18, dash: '4 3',  mark: '!',  name: 'SAR / security' },
    ICE:   { color: '#2088a0', fill: '#2088a0', fillOpacity: 0.14, dash: '6 4',  mark: 'I',  name: 'Ice' },
    OTHER: { color: '#d07020', fill: '#d07020', fillOpacity: 0.16, dash: '6 4',  mark: 'i',  name: 'Other MSI' }
  };

  function msiSubjectOf(f) {
    var s = (f && (f.subject || (f.meta && f.meta.subject))) || 'NAV';
    s = String(s).toUpperCase();
    return MSI_S52[s] ? s : 'OTHER';
  }

  function msiPalette(f) {
    return MSI_S52[msiSubjectOf(f)];
  }

  /** Rough centroid of Polygon / MultiPolygon / LineString for caution mark. */
  function geomCentroid(g) {
    if (!g || !g.coordinates) return null;
    var pts = [];
    function walk(c) {
      if (!c) return;
      if (typeof c[0] === 'number') { pts.push(c); return; }
      for (var i = 0; i < c.length; i++) walk(c[i]);
    }
    walk(g.coordinates);
    if (!pts.length) return null;
    var sx = 0, sy = 0, n = pts.length;
    for (var j = 0; j < n; j++) { sx += pts[j][0]; sy += pts[j][1]; }
    return [sy / n, sx / n]; // latlng
  }

  /**
   * geo — pre-geocoded features (MSI, hazards, areas).
   * Accepts either `latlng: [lat, lon]` or GeoJSON `geometry`, plus popup text.
   * stylePack: 's52-msi' → S-52-inspired caution colours + «!» marks (awareness UI).
   */
  registerKind('geo', function (group, feats, cfg, ctx) {
    var rendered = 0, skipped = 0, labeled = 0;
    var pack = cfg.stylePack === 's52-msi' || cfg.stylePack === 'msi';
    var bySubject = { NAV: 0, MET: 0, SAR: 0, ICE: 0, OTHER: 0 };

    var baseStyle = Object.assign({
      radius: 5, color: '#c45c26', weight: 1.5, fillColor: '#e87a3a', fillOpacity: 0.85
    }, cfg.style || {});
    var areaStyle = Object.assign({
      color: '#c45c26', weight: 2, fillColor: '#e87a3a', fillOpacity: 0.28, dashArray: null
    }, cfg.style || {});
    var lineStyle = Object.assign({
      color: '#c45c26', weight: 2.5, fillOpacity: 0, opacity: 0.9, dashArray: '6 4'
    }, cfg.style || {});

    // Zoom-dependent in-map labels for areas / tracks (cfg.labelMinZoom, default 5).
    var labelsOn = cfg.labels !== false;
    var labelMinZoom = cfg.labelMinZoom != null ? cfg.labelMinZoom : 5;
    var labelPointMinZoom = cfg.labelPointMinZoom != null ? cfg.labelPointMinZoom : 8;
    var cautionMarks = cfg.cautionMarks !== false && pack;
    var labelTargets = []; // { lyr, f, kind: 'area'|'line'|'point' }

    function popupHtml(f) {
      var title = f.label || f.title || f.id || 'Feature';
      var html = '<div class="skipi-map-geo-pop"><b>' + esc(title) + '</b>';
      var subj = msiSubjectOf(f);
      var pal = MSI_S52[subj];
      html += ' <span class="skipi-msi-badge skipi-msi-' + subj + '">' + esc(pal.name) + '</span>';
      var shape = f.shape || (f.meta && f.meta.shape);
      if (shape) html += ' <span class="skipi-map-geo-meta">[' + esc(shape) + ']</span>';
      if (f.navArea || (f.meta && f.meta.navArea)) {
        html += '<br><span class="skipi-map-geo-meta">NAVAREA '
          + esc(f.navArea || f.meta.navArea)
          + (f.number || (f.meta && f.meta.number) ? ' · ' + esc(f.number || f.meta.number) : '')
          + '</span>';
      }
      var haz = f.hazard || (f.meta && f.meta.hazard);
      if (haz) html += '<br><span class="skipi-map-geo-meta">hazard: ' + esc(haz) + '</span>';
      if (f.source || (f.meta && f.meta.source)) {
        html += '<br><span class="skipi-map-geo-meta">' + esc(f.source || f.meta.source) + '</span>';
      }
      var body = f.text || f.body || (f.meta && f.meta.text) || '';
      if (body) html += '<div class="skipi-map-geo-body">' + esc(String(body).slice(0, 900)) + '</div>';
      if (f.disclaimer !== false) {
        html += '<div class="skipi-map-geo-disc">Awareness only · S-52-inspired UI · not official ECDIS / GMDSS</div>';
      }
      html += '</div>';
      return html;
    }

    /** Short label for on-map caption (district name / warning id). */
    function labelText(f) {
      var nav = f.navArea || (f.meta && f.meta.navArea);
      var num = f.number || (f.meta && f.meta.number);
      var subj = msiSubjectOf(f);
      var head = subj === 'NAV' ? '' : (subj + ' ');
      if (nav && num) return (head + nav + ' ' + num).slice(0, 42);
      if (num) return (head + num).slice(0, 42);
      var t = f.label || f.title || (f.meta && f.meta.title) || f.id || '';
      t = String(t).replace(/\s+/g, ' ').trim();
      if (t.length > 42) t = t.slice(0, 40) + '…';
      return (head + (t || 'MSI')).trim();
    }

    function bindPopup(lyr, f) {
      lyr.bindPopup(popupHtml(f), { maxWidth: 320, className: 'skipi-map-geo-popup' });
      lyr.on('click', function () { ctx.emitSelect(cfg.id, f); });
    }

    function setAreaLabel(lyr, f, permanent) {
      if (!lyr.bindTooltip) return;
      if (lyr.unbindTooltip) try { lyr.unbindTooltip(); } catch (e) { /* stub */ }
      var text = labelText(f);
      var subj = msiSubjectOf(f);
      lyr.bindTooltip(esc(text), {
        permanent: !!permanent,
        direction: permanent ? 'center' : 'top',
        className: 'skipi-map-geo-label skipi-msi-' + subj + (permanent ? ' is-on' : ''),
        opacity: 0.95,
        sticky: !permanent
      });
      if (permanent && lyr.openTooltip) {
        try { lyr.openTooltip(); } catch (e) { /* ok */ }
      }
    }

    function syncLabels() {
      if (!labelsOn || !ctx.map || typeof ctx.map.getZoom !== 'function') return;
      var z = ctx.map.getZoom();
      var showArea = z >= labelMinZoom;
      var showPoint = z >= labelPointMinZoom;
      labeled = 0;
      labelTargets.forEach(function (item) {
        var show = item.kind === 'point' ? showPoint : showArea;
        setAreaLabel(item.lyr, item.f, show);
        if (show) labeled++;
      });
    }

    function trackLabel(lyr, f, kind) {
      if (!labelsOn) return;
      labelTargets.push({ lyr: lyr, f: f, kind: kind || 'area' });
    }

    function stylesFor(f) {
      if (!pack) {
        return { point: baseStyle, area: areaStyle, line: lineStyle };
      }
      var p = msiPalette(f);
      return {
        point: Object.assign({
          radius: 6, color: p.color, weight: 2, fillColor: p.fill, fillOpacity: 0.9
        }, cfg.style || {}),
        area: Object.assign({
          color: p.color, weight: 2, fillColor: p.fill, fillOpacity: p.fillOpacity,
          dashArray: p.dash, opacity: 0.95
        }, cfg.style || {}),
        line: Object.assign({
          color: p.color, weight: 2.5, fillOpacity: 0, opacity: 0.95, dashArray: p.dash
        }, cfg.style || {})
      };
    }

    function addCautionMark(latlng, f) {
      if (!cautionMarks || !ctx.L.divIcon || !ctx.L.marker) return;
      var p = msiPalette(f);
      var subj = msiSubjectOf(f);
      var icon = ctx.L.divIcon({
        className: '',
        html: '<div class="skipi-msi-caution skipi-msi-' + subj + '" title="' + esc(p.name) + '">'
          + esc(p.mark) + '</div>',
        iconSize: [18, 18],
        iconAnchor: [9, 9]
      });
      var m = ctx.L.marker(latlng, { icon: icon, interactive: true, keyboard: false });
      bindPopup(m, f);
      group.addLayer(m);
    }

    function addPoint(latlng, f) {
      var st = stylesFor(f).point;
      var m = ctx.L.circleMarker(latlng, st);
      bindPopup(m, f);
      if (cfg.labelPoints) trackLabel(m, f, 'point');
      group.addLayer(m);
      if (pack && cautionMarks) addCautionMark(latlng, f);
      rendered++;
    }

    function addPath(geojsonGeom, f, style, kind) {
      try {
        var layer = ctx.L.geoJSON(
          { type: 'Feature', geometry: geojsonGeom, properties: f },
          {
            style: function () { return style; },
            onEachFeature: function (_feat, lyr) {
              bindPopup(lyr, f);
              trackLabel(lyr, f, kind || 'area');
            }
          }
        );
        group.addLayer(layer);
        if (pack && cautionMarks && (kind === 'area' || kind === 'line')) {
          var c = geomCentroid(geojsonGeom);
          if (c) addCautionMark(c, f);
        }
        rendered++;
        return true;
      } catch (e) {
        skipped++;
        return false;
      }
    }

    // Drop previous zoom listener tied to this group (re-render safe).
    if (group._skipiLabelZoom && ctx.map && ctx.map.off) {
      ctx.map.off('zoomend', group._skipiLabelZoom);
      ctx.map.off('zoom', group._skipiLabelZoom);
    }

    // Optional filter by subject list: cfg.subjects: ['NAV','SAR']
    var subjectFilter = null;
    if (cfg.subjects && cfg.subjects.length) {
      subjectFilter = {};
      cfg.subjects.forEach(function (s) { subjectFilter[String(s).toUpperCase()] = true; });
    }

    feats.forEach(function (f) {
      if (!f) { skipped++; return; }

      var subj = msiSubjectOf(f);
      if (subjectFilter && !subjectFilter[subj]) { skipped++; return; }
      bySubject[subj] = (bySubject[subj] || 0) + 1;

      // Explicit latlng
      if (f.latlng && f.latlng.length >= 2) {
        addPoint(f.latlng, f);
        return;
      }

      var g = f.geometry;
      if (!g || !g.type) { skipped++; return; }
      var st = stylesFor(f);

      if (g.type === 'Point' && g.coordinates) {
        addPoint([g.coordinates[1], g.coordinates[0]], f);
        return;
      }
      if (g.type === 'MultiPoint' && g.coordinates) {
        g.coordinates.forEach(function (c) {
          addPoint([c[1], c[0]], f);
        });
        return;
      }
      if (g.type === 'Polygon' || g.type === 'MultiPolygon') {
        addPath(g, f, st.area, 'area');
        return;
      }
      if (g.type === 'LineString' || g.type === 'MultiLineString') {
        addPath(g, f, st.line, 'line');
        return;
      }
      skipped++;
    });

    if (labelsOn && ctx.map && ctx.map.on) {
      group._skipiLabelZoom = syncLabels;
      ctx.map.on('zoomend', group._skipiLabelZoom);
      syncLabels();
    }

    // Legend panel (once per layer id)
    if (pack && cfg.legend !== false && ctx.host && ctx.host.toggleUI !== false) {
      buildMsiLegend(ctx, cfg, bySubject);
    }

    return {
      rendered: rendered,
      skipped: skipped,
      labeled: labeled,
      labelMinZoom: labelMinZoom,
      stylePack: pack ? 's52-msi' : null,
      bySubject: bySubject
    };
  });

  function buildMsiLegend(ctx, cfg, counts) {
    var doc = global.document;
    if (!doc || !doc.createElement) return;
    var mapEl = ctx.map && ctx.map.getContainer ? ctx.map.getContainer() : null;
    // Prefer parent .skipi-map
    var root = mapEl && mapEl.parentElement ? mapEl.parentElement : mapEl;
    if (!root || !root.appendChild) return;
    var id = 'skipi-msi-legend-' + (cfg.id || 'msi');
    var old = doc.getElementById(id);
    if (old && old.parentNode) old.parentNode.removeChild(old);
    var box = doc.createElement('div');
    box.id = id;
    box.className = 'skipi-msi-legend';
    var title = doc.createElement('div');
    title.className = 'skipi-msi-legend-title';
    title.textContent = 'MSI (S-52-inspired)';
    box.appendChild(title);
    Object.keys(MSI_S52).forEach(function (k) {
      var n = counts[k] || 0;
      if (!n && cfg.legendShowEmpty === false) return;
      var row = doc.createElement('div');
      row.className = 'skipi-msi-legend-row';
      var sw = doc.createElement('span');
      sw.className = 'skipi-msi-swatch skipi-msi-' + k;
      sw.textContent = MSI_S52[k].mark;
      row.appendChild(sw);
      var lab = doc.createElement('span');
      lab.textContent = MSI_S52[k].name + (n ? ' · ' + n : '');
      row.appendChild(lab);
      box.appendChild(row);
    });
    var note = doc.createElement('div');
    note.className = 'skipi-msi-legend-note';
    note.textContent = 'Not certified ECDIS symbology';
    box.appendChild(note);
    root.appendChild(box);
  }

  // ---------------------------------------------------------------- mount ----

  function mount(container, host) {
    host = host || {};
    var L = global.L;
    if (!L) throw new Error('SkipiMap.mount: Leaflet (global L) must be loaded before map-core.js');
    var doc = global.document;
    var el = typeof container === 'string' ? doc.querySelector(container) : container;
    if (!el) throw new Error('SkipiMap.mount: container not found');

    var gaz = (global.SkipiMap && global.SkipiMap.gazetteer);
    if (!gaz) throw new Error('SkipiMap.mount: gazetteer.js must be loaded before map-core.js');
    if (host.gazetteer) gaz.merge(host.gazetteer.ports, host.gazetteer.countries);

    var map = L.map(el, {
      worldCopyJump: true,
      minZoom: host.minZoom != null ? host.minZoom : 1,
      maxZoom: host.maxZoom != null ? host.maxZoom : 12,
      zoomSnap: 0.25,
      zoomDelta: 0.5,
      attributionControl: !!host.tileUrl,
      preferCanvas: host.preferCanvas !== false
    }).setView(
      host.center || [25, 15],
      host.zoom != null ? host.zoom : 2
    );

    // Optional ocean / page background class for themes (maritime dark etc.)
    if (el.classList) {
      el.classList.add('skipi-map');
      if (host.theme === 'maritime' || host.theme === 'dark') el.classList.add('skipi-map-theme-' + host.theme);
    }

    var groups = {}, cfgs = {};
    var ctx = {
      L: L, map: map, gazetteer: gaz, host: host, worldGeo: null,
      t: host.t || function (s) { return s; },
      toast: host.toast || function () {},
      emitSelect: function (layerId, feature) {
        var c = cfgs[layerId];
        if (c && c.onSelect) c.onSelect(feature);
        if (host.onSelect) host.onSelect(layerId, feature);
      }
    };

    function renderLayer(id) {
      var cfg = cfgs[id], g = groups[id];
      if (!cfg || !g) return Promise.resolve();
      // Clean zoom label listeners before re-render
      if (g._skipiLabelZoom && map.off) {
        map.off('zoomend', g._skipiLabelZoom);
        map.off('zoom', g._skipiLabelZoom);
        g._skipiLabelZoom = null;
      }
      g.clearLayers();
      return Promise.resolve(cfg.provider ? cfg.provider() : []).then(function (feats) {
        var fn = KINDS[cfg.kind];
        if (!fn) { ctx.toast('SkipiMap: unknown layer kind "' + cfg.kind + '"', 'error'); cfg._stats = { error: 'unknown-kind' }; return; }
        cfg._stats = fn(g, feats || [], cfg, ctx) || {};
      }).catch(function (e) { ctx.toast('' + e, 'error'); cfg._stats = { error: '' + e }; });
    }

    function buildToggle() {
      if (host.toggleUI === false || !doc.createElement) return;
      var panel = doc.createElement('div');
      panel.className = 'skipi-map-layers';
      Object.keys(cfgs).forEach(function (id) {
        var cfg = cfgs[id];
        var row = doc.createElement('label');
        row.className = 'skipi-map-layer-row';
        var cb = doc.createElement('input');
        cb.type = 'checkbox';
        cb.checked = cfg.visible !== false;
        cb.addEventListener('change', function () { instance.setLayerVisible(id, cb.checked); });
        row.appendChild(cb);
        row.appendChild(doc.createTextNode(' ' + (cfg.label || id)));
        panel.appendChild(row);
      });
      el.appendChild(panel);
    }

    function fetchJson(url) {
      var f = host.fetch || global.fetch;
      return f(url).then(function (r) {
        // Accept real Response (has ok) and test stubs (only json()).
        if (!r || typeof r.json !== 'function') throw new Error('bad fetch response');
        if (r.ok === false) throw new Error('HTTP ' + (r.status || '?'));
        return r.json();
      });
    }

    /** Derive sibling path: leaflet/world.geojson → leaflet/world-land.geojson */
    function siblingWorld(name) {
      var base = host.worldGeoJsonUrl || 'leaflet/world.geojson';
      if (/world[^/]*\.geojson$/i.test(base)) {
        return base.replace(/world[^/]*\.geojson$/i, name);
      }
      // last path segment replace
      return base.replace(/[^/]+$/, name);
    }

    /**
     * Base map:
     *  1) optional raster tiles (host.tileUrl)
     *  2) land fill from detailed land polygons (world-land) — better sea contour
     *  3) coastline stroke (world-coastline) — crisp shore line
     * Countries geojson (world.geojson) kept for regions choropleth only.
     */
    function paintBase(countriesGj, landGj, coastGj) {
      ctx.worldGeo = countriesGj; // regions kind

      if (host.tileUrl && L.tileLayer) {
        try {
          L.tileLayer(host.tileUrl, Object.assign({
            maxZoom: host.maxZoom != null ? host.maxZoom : 12,
            attribution: host.tileAttribution || ''
          }, host.tileOptions || {})).addTo(map);
        } catch (e) {
          ctx.toast('tile layer failed: ' + e, 'error');
        }
      }

      var hasTiles = !!(host.tileUrl);
      var maritime = host.theme === 'maritime' || host.theme === 'dark';
      var canvas = (L.canvas && host.preferCanvas !== false) ? L.canvas({ padding: 0.5 }) : undefined;

      // Prefer dedicated land polygons for shoreline quality; fall back to countries.
      var landData = landGj || countriesGj;

      var landStyle = Object.assign(
        maritime
          ? {
              color: 'transparent',
              weight: 0,
              fillColor: hasTiles ? 'rgba(18, 32, 48, 0.45)' : '#152233',
              fillOpacity: hasTiles ? 0.55 : 1,
              opacity: 1
            }
          : {
              color: 'transparent',
              weight: 0,
              fillColor: hasTiles ? 'rgba(245, 248, 252, 0.35)' : '#e4ecf4',
              fillOpacity: hasTiles ? 0.55 : 1,
              opacity: 1
            },
        host.baseStyle || {}
      );

      L.geoJSON(landData, {
        style: function () { return landStyle; },
        interactive: false,
        renderer: canvas
      }).addTo(map);

      // Dedicated coastline polyline — the “sea contour” the eye follows.
      if (coastGj && host.coastline !== false) {
        var coastStyle = Object.assign(
          maritime
            ? {
                color: hasTiles ? 'rgba(160, 190, 215, 0.75)' : '#6d8eaa',
                weight: hasTiles ? 1.0 : 1.15,
                opacity: 1,
                fillOpacity: 0,
                lineJoin: 'round',
                lineCap: 'round'
              }
            : {
                color: hasTiles ? 'rgba(60, 90, 120, 0.55)' : '#6a849e',
                weight: hasTiles ? 0.9 : 1.05,
                opacity: 1,
                fillOpacity: 0,
                lineJoin: 'round',
                lineCap: 'round'
              },
          host.coastStyle || {}
        );
        L.geoJSON(coastGj, {
          style: function () { return coastStyle; },
          interactive: false,
          renderer: canvas
        }).addTo(map);
      } else if (!coastGj && landData) {
        // Fallback: thin stroke on land if no separate coastline file
        var edge = Object.assign({}, landStyle, {
          color: maritime ? '#6d8eaa' : '#6a849e',
          weight: 1.0,
          fillOpacity: 0
        });
        L.geoJSON(landData, {
          style: function () { return edge; },
          interactive: false,
          renderer: canvas
        }).addTo(map);
      }
    }

    var worldUrl = host.worldGeoJsonUrl || 'leaflet/world.geojson';
    var landUrl = host.landGeoJsonUrl != null ? host.landGeoJsonUrl : siblingWorld('world-land.geojson');
    var coastUrl = host.coastlineGeoJsonUrl != null ? host.coastlineGeoJsonUrl : siblingWorld('world-coastline.geojson');

    var ready = fetchJson(worldUrl).then(function (countries) {
      // Land + coastline are best-effort: if missing, base still works on countries.
      var landP = landUrl
        ? fetchJson(landUrl).catch(function () { return null; })
        : Promise.resolve(null);
      var coastP = (coastUrl && host.coastline !== false)
        ? fetchJson(coastUrl).catch(function () { return null; })
        : Promise.resolve(null);
      return Promise.all([Promise.resolve(countries), landP, coastP]);
    }).then(function (pair) {
      paintBase(pair[0], pair[1], pair[2]);
      (host.layers || []).forEach(function (cfg) {
        cfgs[cfg.id] = cfg;
        var g = L.layerGroup();
        groups[cfg.id] = g;
        if (cfg.visible !== false) g.addTo(map);
      });
      buildToggle();
      return Promise.all(Object.keys(cfgs).map(renderLayer)).then(function () { return instance; });
    }).catch(function (e) { (host.toast || (global.console && global.console.error) || function () {})('SkipiMap base load failed: ' + e); throw e; });

    var instance = {
      version: VERSION,
      map: map,
      ready: ready,
      layers: function () {
        return Object.keys(cfgs).map(function (id) {
          return { id: id, label: cfgs[id].label, kind: cfgs[id].kind, visible: map.hasLayer(groups[id]), stats: cfgs[id]._stats };
        });
      },
      setLayerVisible: function (id, v) {
        var g = groups[id]; if (!g) return;
        if (v) { g.addTo(map); } else { map.removeLayer(g); }
      },
      refresh: function (id) { return id ? renderLayer(id) : Promise.all(Object.keys(cfgs).map(renderLayer)); },
      destroy: function () { try { map.remove(); } catch (e) {} }
    };
    return instance;
  }

  global.SkipiMap = global.SkipiMap || {};
  global.SkipiMap.version = VERSION;
  global.SkipiMap.registerKind = registerKind;
  global.SkipiMap.kinds = KINDS;
  global.SkipiMap.mount = mount;
})(typeof window !== 'undefined' ? window : globalThis);
